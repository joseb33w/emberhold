# Godot
.godot/
/out/
export_presets.cfg.bak
*.uid
*.import

# Heavy CC0 binary assets — regenerated by ./fetch_assets.sh + tools/gen_audio.py.
# The deployed web build bakes them in; keep the source repo lean.
/models/
/audio/
/textures/

# Local toolchain / scratch (never commit the engine binary)
/godot
/playbooks/
/verify/
/test/
asset-manifest.json
*.tmp
